<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base App Theme (Used for Welcome & Settings) -->
    <style name="Theme.Bixby" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/one_ui_black</item>
        <item name="android:navigationBarColor">@color/one_ui_black</item>
        <item name="android:windowBackground">@color/one_ui_black</item>
    </style>

    <!-- CRITICAL: Transparent Overlay Theme for Main Assistant Screen (1:1 Clone effect) -->
    <style name="Theme.Bixby.Overlay" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.7</item>
    </style>
</resources>
